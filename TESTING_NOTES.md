# 微信视频通话插件测试记录

## 2026-09-21 首次打通（魅族 20 + 微信 8.0.78，全流程 ✅）

- 当天最后一轮自驱动测试：步骤 1~7 全部 `completed`，截屏确认进入视频通话呼叫界面
  （张三头像 + “等待对方接受邀请”），随后挂断回到聊天页。
- 最终链路（全部经 `dispatchGesture`，零节点依赖）：
  主页→切微信 Tab→搜索图标→点搜索框→长按→点“粘贴”气泡（程序实测中心 148,356）
  →点第一个结果→聊天页→+号（延时 1.5s 等转场）→视频通话菜单→确认框→拨出。
- 关键教训：
  1. `Runtime.exec("input …")` 在 App 内无权限，必然失败；`dispatchGesture` 才是正道。
  2. 聊天页转场动画期间的手势会被吞，+ 号点击必须延时。
  3. `adb shell input tap` 在本机有约 116px 上偏（点 (540,815) 实际落在 ~699），
     调试触发 example 按钮时要 +116 修正；插件自身手势无此问题。
  4. 挂断红键实测约 (540,2045)，已更新默认值；`hangUp()` 可用。
- 待做：语音通话确认框坐标（640,1770）尚未实测验证；多机型/多微信版本适配。

---

## 2026-09-21 修订说明（相对 0001-8.0.78-20.patch 的改动）

上一版 patch 在真机上没调通，根因定位如下：

1. **`Runtime.exec("input tap/text")` 在 App 进程内无权限**：`input` 命令需要
   `INJECT_EVENTS`，只有 `adb shell` / root 才有。`adb shell input tap` 验证通过
   不等于 App 内可用。已改为无障碍服务自带的 `dispatchGesture` 注入手势
   （`canPerformGestures=true`，minSdk 24 满足 API 24+ 要求），点击不再依赖节点树。
2. **输入改三级回退**：`ACTION_SET_TEXT`（支持中文 Unicode，可直接输中文备注，
   不必转拼音）→ 遍历 EditText → 剪贴板粘贴。不再用 `input text`。
3. **去掉 `Thread.sleep`**：回调线程阻塞会导致事件堆积/ANR，改 `Handler.postDelayed`
   定时链；+ 菜单/确认框是同 Activity 弹窗（不一定有 WINDOW_STATE_CHANGED），
   步骤 6/7 改定时驱动，不再等事件。
4. **坐标相对化**：`WeChatCoords` 存 0..1 相对坐标，按屏幕宽高换算；
   Dart 侧 `setCoordinate(key, fx, fy)` 可运行时覆盖校准，不用重新编译。
5. **状态机硬化**：45s 超时会看门狗、步骤冷却去重、新增 `cancel()`；
   微信未安装时直接失败并终止会话；xml 监听收敛回
   `typeWindowStateChanged|typeWindowContentChanged + packageNames=com.tencent.mm`。
6. 新增 `hangUp()`（点挂断按钮坐标，需按机型校准）。

待真机验证（本机无 Android SDK，请在有 adb 的机器上按下面的调试指令抓日志回传）：
- `adb logcat -s WechatAccessibilityTag`
- 重点看 `tap[...] completed/cancelled`、`Step3: input` 的 `setText/pasteText ok=`、
  以及各 `StepN` 是否按顺序推进。
- 若某步点偏：在微信对应页面用“指针位置”（开发者选项）读出实际像素，
  除以屏幕宽高得 fx/fy，调用 `setCoordinate` 覆盖验证，确认后再写进默认值。

---
## 测试设备
- **手机**: 魅族 20 (MEIZU 20)
- **系统**: Android 16, API 36
- **屏幕分辨率**: 1080 x 2400
- **屏幕密度**: 450 dpi

## 微信版本
- **版本号**: 8.0.78
- **版本代码**: 3180
- **APK 路径**: `/tmp/wechat_base.apk`

## 无障碍服务封锁情况

### 已确认的封锁行为
| 功能 | 状态 | 说明 |
|------|------|------|
| `uiautomator dump` | ❌ 被封锁 | 返回空树，只有一个根节点 |
| `rootInActiveWindow` | ❌ 被封锁 | 返回的根节点无子节点，className=null |
| `findAccessibilityNodeInfosByText` | ⚠️ 部分有效 | 能找到部分元素（如搜索、微信、通讯录等），但所有节点的 bounds 均为 Rect(0,0-0,0) |
| `findAccessibilityNodeInfosByViewId` | ⚠️ 部分有效 | 能找到部分元素（如 icon_tv、jha），但 bounds 同样为零 |
| `performAction(ACTION_CLICK)` | ❌ 无效 | 返回 true 但实际不执行点击 |
| `performAction(ACTION_SET_TEXT)` | ❌ 无效 | 无法通过无障碍设置文本 |
| 无障碍事件 (AccessibilityEvent) | ✅ 正常 | 仍能接收 TYPE_WINDOW_STATE_CHANGED 等事件 |

### 被封锁的 Activity 类名
- `com.tencent.mm.ui.LauncherUI` (主界面)
- `com.tencent.mm.plugin.search.ui.FTSMainUI` (搜索页面)
- `com.tencent.mm.chat.ui.ChatUI` (聊天页面)

## 已验证可用的方案

### 1. 坐标点击方案 (`input tap`)
所有 UI 交互必须通过 `adb shell input tap x y` 坐标点击实现。

### 2. 拼音搜索方案 (`input text`)
微信搜索支持拼音匹配，可直接输入拼音搜索中文联系人：
```
adb shell input text 'zhangsan'
```
微信会自动匹配「张三」等联系人，无需输入中文。

### 3. 无障碍事件监听
仍可通过 `onAccessibilityEvent()` 监听窗口变化，用于判断当前页面状态。

## 验证通过的完整流程

### 测试联系人: 张三
### 测试时间: 2026-09-19

| 步骤 | 操作 | 坐标/命令 | 状态 |
|------|------|-----------|------|
| 1 | 打开微信 | `am start -n com.tencent.mm/.ui.LauncherUI` | ✅ |
| 2 | 点击搜索图标 | `input tap 892 191` | ✅ |
| 3 | 输入拼音 | `input text 'zhangsan'` | ✅ |
| 4 | 点击搜索结果(张三) | `input tap 450 420` | ✅ |
| 5 | 点击+号 | `input tap 1013 2316` | ✅ |
| 6 | 点击视频通话(+菜单) | `input tap 659 1723` | ✅ |
| 7 | 点击视频通话(对话框) | `input tap 640 2038` | ✅ |

## 魅族 20 坐标点详情

### 屏幕信息
- 分辨率: 1080 x 2400
- getevent 原始坐标范围: 0-1080 (X), 0-2400 (Y)
- 坐标与屏幕像素一一对应，无需额外转换

### 已确认的 UI 元素坐标

#### 微信主页面
| 元素 | X | Y | 说明 |
|------|---|---|------|
| 搜索图标 | 892 | 191 | 右上角放大镜 |
| +号按钮 | ~1013 | ~145 | 右上角加号 |

#### 搜索页面
| 元素 | X | Y | 说明 |
|------|---|---|------|
| 搜索输入框 | - | - | 已获取焦点，直接 input text |
| 搜索结果(第一个) | 450 | 420 | 联系人列表第一项 |

#### 聊天页面
| 元素 | X | Y | 说明 |
|------|---|---|------|
| +号按钮 | 1013 | 2316 | 右下角加号 |

#### +菜单
| 元素 | X | Y | 说明 |
|------|---|---|------|
| 相册 | ~135 | ~1380 | 第一行第一列 |
| 拍摄 | ~340 | ~1380 | 第一行第二列 |
| 视频通话 | 659 | 1723 | 第一行第三列 |
| 位置 | ~745 | ~1380 | 第一行第四列 |
| 红包 | ~135 | ~1580 | 第二行第一列 |
| 礼物 | ~340 | ~1580 | 第二行第二列 |
| 转账 | ~540 | ~1580 | 第二行第三列 |
| 语音输入 | ~745 | ~1580 | 第二行第四列 |

#### 确认对话框
| 元素 | X | Y | 说明 |
|------|---|---|------|
| 视频通话 | 640 | 2038 | 对话框第一项 |
| 语音通话 | ~640 | ~1770 | 对话框第二项 |
| 取消 | ~540 | ~1910 | 对话框第三项 |

#### 视频通话界面
| 元素 | X | Y | 说明 |
|------|---|---|------|
| 挂断按钮 | 540 | 1800 | 底部红色按钮 |

## 注意事项

1. **坐标偏移问题**: `input tap` 的坐标可能存在状态栏偏移（状态栏高度约 116px），实际点击位置可能比预期高
2. **搜索结果动态性**: 搜索结果的坐标取决于搜索词匹配结果，第一个结果的位置可能变化
3. **+菜单坐标**: +菜单会根据聊天内容动态调整，坐标需要验证
4. **拼音输入限制**: `input text` 只支持 ASCII 字符，中文必须用拼音替代

## 建议的改进方向

1. 将坐标定义为常量，便于不同设备适配
2. 增加坐标偏移量配置，适配不同状态栏高度
3. 使用拼音输入替代中文输入，避免输入法问题
4. 增加页面状态检测逻辑，确保在正确的页面执行操作
