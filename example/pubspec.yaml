name: wechat_video_call_example
description: Demo for wechat_video_call plugin — calibration wizard + dial flow.
publish_to: 'none'
version: 2.0.2

environment:
  sdk: ^3.13.4

dependencies:
  flutter:
    sdk: flutter
  wechat_video_call:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
